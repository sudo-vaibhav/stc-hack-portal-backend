user/profile
        uuid
        auth info
        bio
        actual name
        skills
        college
        teams array (a list of team ids of which this user is a part)
        external links (linkedin, portfolio, website) //optional


events
        uuid
        description
        name of event
        date ( probably be a range of two timestamps)
        url
        deadlines (multiple)  //first could be deadline for registration
                              // second could be deadline for idea submissions
        required criteria (optional)
        team size
        particpation fees (per head)
        prizes
        
        teams
                uuid
                members
                description
                domain
                url( optional )


